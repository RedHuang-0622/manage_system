---

### 三、 项目演示视频脚本 (3-5分钟)

提示：答辩和演示占了极大的权重（15分+演示流程），流畅的表达和针对评分点的精准打击是高分关键。使用 Postman 或 Swagger 进行接口调用演示即可。

**【0:00 - 0:30】开场与业务背景**
“老师好，我是软件工程专业的黄子然。我演示的项目是《实验室资产与人员管理系统》。考虑到日常实验室中，双负责人制下的设备借用和人员权限管理比较复杂，我设计了这个具有完整业务闭环的纯后端系统。项目严格采用了 Gin、GORM、Redis 等课程指定的全部核心技术栈。”

**【0:30 - 1:30】架构规范与配置演示 (拿工程规范分)** “首先展示代码规范。项目采用标准的 Controller、Service、Dao 分层架构。
环境配置使用了 **Viper** 实现全分离，这里是 `config.yaml`。
在异常处理方面，我封装了全局的 Recovery 中间件，同时接入了 **Zap** 日志。大家看控制台和 log 文件夹，所有的接口请求、数据库慢查询以及异常报错，都已经实现了结构化记录和按天自动切割日志。”

**【1:30 - 2:30】认证、权限与参数校验 (拿功能满分)** 
“接下来演示核心接口。
首先我通过 Postman 发送登录请求，账号密码验证后返回 **JWT** Token。
对于传入的参数，我在实体类使用了 **Validator** 进行强校验，如果格式不对会在控制层直接拦截。
系统集成了 **Casbin** 进行 RBAC 权限控制。现在我用‘普通成员’的 Token 尝试调用‘负责人审核’接口，系统返回 403 越权提示，鉴权拦截生效。”

**【2:30 - 3:30】核心闭环：事务与缓存优化 (拿高阶优化分)** 
“最后演示核心的设备借阅闭环。
当首页高频请求‘可用设备列表’时，为了减轻数据库压力，系统会优先从 **Redis** 中读取缓存数据。
当我发起借阅申请并由负责人审批通过时，系统在 **GORM** 中开启了数据库事务（Transaction），确保‘生成借阅工单’和‘扣减设备可用库存’这两个操作同时成功或同时失败，保证数据的一致性。同时，底层也对高频查询字段建立了索引，并配置了数据库连接池来优化性能。”

**【3:30 - 4:00】总结**
“整个项目完整覆盖了配置分离、多表事务、Redis缓存、权限控制等课程全部考查点，代码均添加了详细注释，项目可稳定独立运行。我的演示完毕，谢谢老师。”

---

这个方案利用了你对“实验室管理与负责人制度”天然的敏锐度，能将业务逻辑最简化，同时技术落点每一锤都砸在评分标准（54-60分档次）的得分区上。你觉得针对这个系统，Redis 除了做设备列表缓存，是否还需要我为你设计一套 JWT Token 的黑名单（登出失效）机制？