# 04 — 基础设施模块测试

## 1 测试范围

| 组件 | 测试内容 |
|------|---------|
| Recovery 中间件 | Panic 捕获、堆栈记录、500 响应 |
| Response 包 | Success / Created / Error / PageResult 格式 |
| Errcode 包 | 错误码映射、消息获取 |
| Config 包 | Viper 加载、环境变量覆盖、默认值 |
| Logger 中间件 | 请求日志字段完整性 |
| Redis 连接 | 连接成功、Ping |

## 2 Recovery 中间件测试

| 编号 | 测试用例 | 模拟操作 | 预期 |
|------|---------|---------|------|
| REC-01 | 正常请求无 Panic | 正常 Handler | 200 正常响应 |
| REC-02 | Panic 被捕获 | Handler 中主动 panic("test") | 500 {code:5000, msg:"系统内部错误"}, 进程不崩溃 |
| REC-03 | Panic 堆栈记录 | Handler 中主动 panic | Zap Error 日志包含 stack 字段 |
| REC-04 | Panic 日志含请求信息 | 触发 panic | 日志含 path, method, ip 字段 |
| REC-05 | Panic 后终止后续处理 | 触发 panic | c.AbortWithStatusJSON 被调用，不继续执行 |
| REC-06 | 嵌套 Panic | 中间件层 panic | 仍被 Recovery 捕获 |

**测试实现：**
```go
func TestRecovery_PanicCaptured(t *testing.T) {
    buf := &bytes.Buffer{}
    logger := zap.New(zapcore.NewCore(
        zapcore.NewJSONEncoder(zapcore.EncoderConfig{
            TimeKey: "ts", MessageKey: "msg", LevelKey: "level",
            EncodeTime: zapcore.ISO8601TimeEncoder,
            EncodeLevel: zapcore.LowercaseLevelEncoder,
        }),
        zapcore.AddSync(buf), zapcore.DebugLevel,
    ))

    router := gin.New()
    router.Use(Recovery(logger))
    router.GET("/panic", func(c *gin.Context) { panic("test panic") })

    w := httptest.NewRecorder()
    req := httptest.NewRequest("GET", "/panic", nil)
    router.ServeHTTP(w, req)

    assert.Equal(t, 500, w.Code)
    assert.Contains(t, w.Body.String(), "5000")
    assert.Contains(t, w.Body.String(), "系统内部错误")
    assert.Contains(t, buf.String(), "panic_recovered")
    assert.Contains(t, buf.String(), "test panic")
}
```

## 3 Response 包测试

| 编号 | 测试用例 | 输入 | 预期 JSON |
|------|---------|------|----------|
| RSP-01 | Success — object | data={name:"test"} | `{"code":0,"msg":"success","data":{"name":"test"}}` |
| RSP-02 | Success — null | data=nil | `{"code":0,"msg":"success","data":null}` |
| RSP-03 | Created — object | data={id:1} | 201 `{"code":0,"msg":"创建成功","data":{"id":1}}` |
| RSP-04 | Error — 400 参数错误 | (400, 1001, "参数错误") | 400 `{"code":1001,"msg":"参数错误","data":null}` |
| RSP-05 | Error — 401 认证失败 | (401, 2001, "用户名或密码错误") | 401 `{"code":2001,"msg":"用户名或密码错误","data":null}` |
| RSP-06 | Error — 403 权限不足 | (403, 2005, "权限不足") | 403 `{"code":2005,"msg":"权限不足","data":null}` |
| RSP-07 | Error — 500 系统错误 | (500, 5000, "系统内部错误") | 500 `{"code":5000,"msg":"系统内部错误","data":null}` |
| RSP-08 | PageResult — 正常 | total=50, page=2, size=10, list=[...] | `{"code":0,"msg":"success","data":{"total":50,"page":2,"page_size":10,"list":[...]}}` |
| RSP-09 | PageResult — 空列表 | total=0, page=1, size=10, list=[] | `{"code":0,"msg":"success","data":{"total":0,"page":1,"page_size":10,"list":[]}}` |

## 4 Errcode 包测试

| 编号 | 测试用例 | 输入 | 预期 |
|------|---------|------|------|
| ERR-01 | GetMsg — success | code=0 | "success" |
| ERR-02 | GetMsg — 参数错误 | code=1001 | "请求参数错误" |
| ERR-03 | GetMsg — 认证失败 | code=2001 | "用户名或密码错误" |
| ERR-04 | GetMsg — 权限不足 | code=2005 | "权限不足" |
| ERR-05 | GetMsg — 库存不足 | code=3004 | "设备库存不足" |
| ERR-06 | GetMsg — 系统错误 | code=5000 | "系统内部错误" |
| ERR-07 | GetMsg — 未知 code | code=9999 | "未知错误" |
| ERR-08 | 错误码不重复 | 遍历所有常量 | 无重复值 |
| ERR-09 | 错误码分段正确 | 遍历所有常量 | 1xxx→通用, 2xxx→认证, 3xxx→业务, 5xxx→系统 |

## 5 Config 包测试

| 编号 | 测试用例 | 操作 | 预期 |
|------|---------|------|------|
| CFG-01 | Load — 正常加载 | 读 config.test.yaml | 返回完整 Config 结构体 |
| CFG-02 | Load — 文件不存在 | 指定不存在的路径 | panic（启动阶段可接受） |
| CFG-03 | Load — 默认值生效 | 配置中缺少 server.port | 自动设 8080 |
| CFG-04 | Load — 默认值生效 | 缺少 jwt.expire | 自动设 7200 |
| CFG-05 | Load — 默认值生效 | 缺少 jwt.issuer | 自动设 "lab-system" |
| CFG-06 | Load — 环境变量覆盖 | 设 LAB_SERVER_PORT=9090 | Config.Server.Port = 9090 |
| CFG-07 | Load — 环境变量覆盖密码 | LAB_MYSQL_PASSWORD=xxx | Config.MySQL.Password = "xxx" |
| CFG-08 | Load — 环境变量优先于文件 | 文件 port=8080, 环境变量 port=9090 | 最终 port=9090 |
| CFG-09 | MySQL DSN 格式正确 | 拼接 DSN | 格式: `user:pass@tcp(host:port)/db?charset=utf8mb4&parseTime=True&loc=Local` |

## 6 Logger 中间件测试

| 编号 | 测试用例 | 模拟请求 | 预期日志字段 |
|------|---------|---------|------------|
| LOG-01 | 正常请求日志 | POST /api/v1/users | 含 method, path, status, latency_ms, ip |
| LOG-02 | 401 状态码 | 无 Token 请求 | status=401 |
| LOG-03 | 500 状态码 | Panic 触发 | status=500 |
| LOG-04 | latency_ms 格式 | 任意请求 | latency_ms 为 Duration 类型 |
| LOG-05 | 包含 user_id | 已认证请求 | user_id 不为 0 |
| LOG-06 | 未认证请求 user_id | 无 Token | user_id=0 |

## 7 pkg/jwt 包测试

| 编号 | 测试用例 | 输入 | 预期 |
|------|---------|------|------|
| JWT-01 | GenerateToken — 正常 | userID=1, role=super_admin | 返回 JWT 字符串 + expires_in |
| JWT-02 | GenerateToken — Claims 完整性 | — | Token 解析后 user_id, username, role_id, role_name 均正确 |
| JWT-03 | GenerateToken — 过期时间 | expire=7200 | exp - iat ≈ 7200s |
| JWT-04 | GenerateToken — issuer | issuer="lab-system" | Claims.Issuer = "lab-system" |
| JWT-05 | ParseToken — 正常 | 有效 Token | 返回完整 Claims |
| JWT-06 | ParseToken — 过期 | 已过期 Token | 返回错误 |
| JWT-07 | ParseToken — 错误密钥 | 不同 secret 签的 | 返回签名验证错误 |
| JWT-08 | ParseToken — 格式错误 | "not.a.jwt" | 返回解析错误 |
| JWT-09 | ParseToken — 空字符串 | "" | 返回解析错误 |
| JWT-10 | AddToBlacklist — 正常 | 有效 Token | Redis SET 成功，TTL = 剩余有效期 |
| JWT-11 | IsInBlacklist — 存在 | 已加入黑名单的 Token | 返回 true |
| JWT-12 | IsInBlacklist — 不存在 | 未加入的 Token | 返回 false |
| JWT-13 | 黑名单 Key 格式 | Token "abc" | Key = "jwt:blacklist:" + sha256("abc") |
| JWT-14 | 黑名单 TTL 自动过期 | TTL=1s | 等待 2s 后 IsInBlacklist 返回 false |

## 8 Redis 连接测试

| 编号 | 测试用例 | 操作 | 预期 |
|------|---------|------|------|
| RDS-01 | InitRedis — 正常连接 | 有效配置 | 返回 *redis.Client，Ping 成功 |
| RDS-02 | InitRedis — 连接失败 | 无效地址 | panic（启动阶段可接受） |
| RDS-03 | InitRedis — 密码错误 | 错误密码 | Ping 失败 → panic |
| RDS-04 | 连接池配置验证 | PoolSize=100 | client.Options().PoolSize = 100 |
